name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup PHP 8.1
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.1'
        extensions: mbstring, xml, bcmath, mysql, curl, zip, gd, intl, fileinfo
        tools: composer:v2

    - name: Install Composer dependencies
      run: composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader

    - name: Deploy to server
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          set -e
          echo "🚀 Starting deployment..."

          export PATH="$HOME/bin:/usr/local/bin:$PATH"
          cd ${{ secrets.SERVER_PATH }}
          git config --global --add safe.directory ${{ secrets.SERVER_PATH }}

          # Проверяем статус репозитория
          echo "🔍 Checking repository status..."

          # Сбрасываем все незакоммиченные изменения
          echo "🧹 Cleaning local changes..."
          git reset --hard HEAD
          git clean -fd

          # Принудительно обновляем до origin/main
          echo "📥 Updating code..."
          git fetch origin main
          git reset --hard origin/main

          # Убедимся что мы на правильной ветке
          git checkout main 2>/dev/null || git checkout -b main --track origin/main

          # Обновляем submodules если есть
          git submodule update --init --recursive

          # Дальше ваш обычный deployment
          echo "📦 Installing dependencies..."
          composer install --no-dev --optimize-autoloader --no-interaction

          echo "🗄️ Running migrations..."
          php artisan migrate --force

          echo "🧹 Clearing cache..."
          php artisan cache:clear
          php artisan config:clear
          php artisan route:clear
          php artisan view:clear

          echo "⚡ Optimizing..."
          php artisan config:cache
          php artisan route:cache
          php artisan view:cache

          echo "🔒 Setting permissions..."
          chmod -R 755 storage bootstrap/cache

          echo "✅ Deployment completed successfully!"
